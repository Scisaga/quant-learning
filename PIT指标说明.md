# PIT 指标说明

本文说明本项目里“PIT（Point-in-Time）财务指标”相关的字段命名、含义、以及在 Qlib 数据中的落地方式。

## 1. 什么是 PIT

PIT 的核心是：在任一交易日 t，模型只能看到“截至 t 当时已披露”的财务数据（避免未来函数/数据泄露）。
与此对应：
- 非 PIT：可能把后续披露的财务数据回填到过去，产生 look-ahead bias。
- PIT：按公告披露时间推进（或按可得时间推进），历史时点只暴露当时已知信息。

## 2. 本项目的字段约定

训练脚本里默认的 PIT 字段（例）：
- `assettequity_q`
- `netprofit_q`
- `roeavg_q`
- `yoyni_q`

字段后缀 `_q` 表示季度频率来源（最终会被处理成日频可用的特征 bin）。

## 3. Qlib bin 中的落地文件名

本项目使用“离线 dump 的日频 PIT 特征”写入 Qlib 的 `features/<instrument>/` 目录。
默认前缀为 `pit_`，因此对应的 bin 文件形式为：
- `pit_<field>.day.bin`

例如：
- `pit_assettequity_q.day.bin`
- `pit_netprofit_q.day.bin`

训练时通过表达式引用（示意）：`$pit_assettequity_q`。

## 4. 缺失与对齐

现实数据中可能出现：
- 部分股票缺某个 PIT 字段的 bin
- 某些时间段缺数据（上市较晚/停牌/财务披露不足）

建议：
- 训练前先检查目标市场在目标时间段内的 PIT bin 覆盖率。
- 明确处理策略：
  - `error`：发现缺失就中断（保证严格可比）
  - `skip`：过滤掉缺失标的（提升可跑性，但样本集合会变化）

## 5. 常用字段解释（简述）

- `assettequity_q`：资产/权益相关的季度指标（具体口径取决于数据源定义）
- `netprofit_q`：净利润（季度）
- `roeavg_q`：平均 ROE（季度）
- `yoyni_q`：净利润同比增速（季度）

如果你希望更严格的定义（分母、是否合并报表、TTM/单季等），建议把数据源的字段字典补在此文档中。

## 6. TODO

- 在这里补充：数据源字段字典、单位、缩放、缺失值处理、公告日期/可得日期规则。
